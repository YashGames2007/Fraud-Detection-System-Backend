spring.application.name=Astraguard Backend
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

# Server configuration for Railway
server.port=${PORT:8080}
server.servlet.context-path=/

# Database (Railway provides DATABASE_URL)
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# MongoDB (Railway provides MONGODB_URI or use your own)
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://yashgames2007:YashGames2007%40mongodb@upicluster.uytsshc.mongodb.net/?appName=UPICluster}
spring.data.mongodb.database=UPIDemo

# Security
jwt.secret=${JWT_SECRET:THIS_IS_A_DEMO_SECRET_CHANGE_LATER_123456}
jwt.expiration.ms=${JWT_EXPIRATION_MS:3600000}

# ML Service
ml.base-url=${ML_BASE_URL:https://pratikd-110924-fraud-detection-ml-service.hf.space}
ml.evaluate-path=/secure_predict